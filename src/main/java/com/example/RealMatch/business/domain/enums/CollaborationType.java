package com.example.RealMatch.business.domain.enums;

public enum CollaborationType {
    APPLIED,    // 내가 지원한 캠페인 (CampaignApply)
    SENT,       // 내가 보낸 제안 (CampaignProposal - brand)
    RECEIVED    // 내가 받은 제안 (CampaignProposal - creator)
}
